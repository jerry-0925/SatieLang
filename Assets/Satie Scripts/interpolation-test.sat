# Test script for interpolation functionality

# Using goto - goes from 0 to 0.5 and stays at 0.5
loop "music/drone":
    starts_at = 2
    volume = goto(0and0.5 as inquad in 2)
    pitch = gobetween(1.2and2 as incubic in 5)
    move = walk, -20to10, -5to15, 0.5to2
    visual = trail